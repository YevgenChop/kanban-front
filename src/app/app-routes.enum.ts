export enum AppRoutes {
  LogIn = 'auth/login',
  SignUp = 'auth/signup',
  EmailConfirmation = 'auth/email-confirmation',
  MyBoards = 'my-boards',
  Boards = 'boards',
  NewBoard = 'new-board',
  Settings = 'settings',
}
